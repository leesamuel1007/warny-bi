# Local secrets and generated credentials
.env
.env.*
!.env.example
*.key
*.pem
*secret*
*secrets*

# Python/runtime artifacts
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv
venv/
uv.lock
.python-version

# Power BI local/cache artifacts
*.pbitmp
*.pbix.tmp

# Raw documents and processed files
docs/*.docx
docs/*.pdf
data/processed/

# Codex files
*.codex
.agents
AGENTS.md

# Keep raw datasets only when licensing and size policy allow it.
# Use Git LFS for large images and PBIX files.
