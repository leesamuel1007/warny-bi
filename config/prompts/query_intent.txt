Extract structured vehicle warning-light intent from the user query.

Return only one JSON object. Do not include Markdown, explanation, or code fences.
Use null when a field is not stated or strongly implied. Do not guess.

JSON schema:
{
  "make": string or null,
  "model": string or null,
  "model_year": integer or null,
  "warning_light": string or null
}

Guidance:
- Extract make, model, and model_year only when the query states them.
- Normalize warning-light descriptions to short phrases such as "engine light", "oil pressure light", "tire pressure light", "airbag light", "ABS light", or "battery light".
- For recall applicability, missing make, model, or model_year usually matters.
- For general warning-light guidance, missing vehicle fields may be acceptable.
- Do not ask follow-up questions. The downstream answer must respond from the single query and state caveats when important details are missing.

User query:
{query}
